<div class="col-md-12">
  <div class="org">
    <%= link_to("編輯使用單位", edit_admin_org_path(@org), :class => "btn btn-primary pull-right")%> 
  </div>
  <h1> 使用單位 </h1>
  <h2> <%= @org.name %> </h2>
  <p> 收費標準： <%= link_to(@org.identity.classification, admin_identity_path(@org.identity_id)) %> </p>
  <p> 全名： <%= @org.official_name %> </p>
  <p> 地址： <%= @org.address %> </p>
  <p> 電話： <%= @org.tel %> </p>
  <p> 備註： <%= @org.note %> </p>
  <p> 2014年累計總金額： <%= @org.sum_price_of_2014 %> </p>
  
  <div class="member">
    <%= link_to("新增使用者", new_admin_member_path, :class => "btn btn-primary pull-right")%>
  </div>
  <h3> 使用者 共<%= @members.size %>人 </h3>
  <%= render "admin/members/members_list" %>

  <div class="record">
    <%= link_to("新增使用記錄", new_admin_record_path, :class => "btn btn-primary pull-right")%>
  </div>
  <h3> 使用記錄 共<%= @records.size %>筆 </h3>
  <%= render "admin/records/records_list" %>
</div>